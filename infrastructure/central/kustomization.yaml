apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./local-path-provisioner/ks.yaml
configMapGenerator:
- name: cluster-vars
  namespace: flux-system
  literals:
  - DOMAIN=petruzhome.it
  options:
    disableNameSuffixHash: true
