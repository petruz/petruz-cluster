apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: frigate-data
  namespace: home-automation
  annotations:
    volumeType: local
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi
