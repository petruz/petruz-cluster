apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: postgres
  namespace: data-process
spec:
  interval: 30m
  chart:
    spec:
      chart: postgresql
      version: 12.5.2
      interval: 30m
      sourceRef:
        kind: HelmRepository
        name: bitnami
        namespace: flux-system
